[project]
name = "reminder-pal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "apscheduler>=3.11.3",
    "python-telegram-bot[job-queue]>=22.8",
]
